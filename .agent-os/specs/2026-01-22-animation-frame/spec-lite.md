# Spec Summary (Lite)

Build a React + Framer Motion component that replicates the Flecks Login Animation Frame from Figma. The component features a 40-second looping animation with 13 gradient fleck elements moving through 4 position variants, 3 static R3 decorative vectors, and a centered logo overlay, all within a responsive container with overflow masking.
